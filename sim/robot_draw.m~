%%-----------------------------------------------------------
%% (c) Juan Gonzalez-Gomez. (Obijuan)  juan@iearobotics.com
%% Distrubuted under del GPL license
%%-----------------------------------------------------------
%%-- Draw the robot based on position (x,y)
%%-- x,y: position of the end effector in the xy plane
%%-- l: Length of the total manipulator(joint in center)
%%----------------------------------------------------------
function robot_draw(x,y,l)

%%-- Robot data. These points define the robot shape in its home state
p1 = [0,0];
p2 = [l/2,0];
p3 = [l/2,l/2];
p4 = [0,l/2];

%%-- convert the points into two list with the x and y coordinates
%%-- It is necesarry for plotting

%-- Draw the robot
plot(p1,p2,'-r');


