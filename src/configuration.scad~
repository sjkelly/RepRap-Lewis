/*
Configuration for a RepRap Lewis. Derived parameterizations occur in "measurements.scad".
*/
//What you want
build_radius = 100; //in millimeters
build_height = 100; //in millimeters

//What you bring to the party
build_bed_thick = 4; //thickness of the build bed in millimeters
screw = "M3"; //Values are M2, M2.5, M3, M4, M5, M6, M8
lead_screw = "M4"; //Values are M2, M2.5, M3, M4, M5, M6, M8
motor = "NEMA14"; //Values are NEMA8, NEMA11, NEMA14, NEMA17 
bearing = "624";
